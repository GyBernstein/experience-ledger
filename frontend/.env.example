# Server-side Vite proxy target. This value does not enter the browser bundle.
LEDGER_API_TARGET=http://127.0.0.1:8080
# Do not put Bearer tokens in VITE_* variables. Enter the token on the connection page.
