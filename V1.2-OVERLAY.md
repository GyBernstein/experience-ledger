# Experience Ledger V1.2 覆盖说明

本覆盖包用于已经叠加 V1.1、Human Judgment 功能并升级到 Spring Boot 4.1.1 的本地工程。

## 覆盖方式

1. 先备份本地工程和数据库；
2. 将压缩包内容解压到 Experience Ledger 项目根目录，允许覆盖同名文件；
3. 保留你本地现有的 `pom.xml`。本包故意不包含该文件，不会把 Spring Boot 4.1.1 降回旧基线；
4. 检查本地已有 Flyway `V4`，本次新增迁移固定为 `V5__ai_assisted_authoring.sql`；
5. 重新构建前后端并启动，Flyway 会自动新增 Authoring 表，不删除 V1/V1.1 数据。

```bash
mvn clean verify
cd frontend && npm ci && npm test && npm run build
```

如果本地已经存在另一个 Flyway V5，请先把本包迁移和文件名整体顺延到下一个空闲版本，不要修改已经在任何环境执行过的 migration。

## 启用 LLM

不配置时使用 `local-safe`，便于先验证完整流程且不会发送外部请求。启用 OpenAI-compatible/Ollama：

```bash
LEDGER_AI_ENABLED=true
LEDGER_AI_PROVIDER=openai-compatible
LEDGER_AI_URL=http://localhost:11434/v1/chat/completions
LEDGER_AI_API_KEY=
LEDGER_AI_MODEL=qwen3:14b
```

Docker Compose 使用者可同时叠加包内的 `.env.example` / `docker-compose.yml` 配置项。

## 首次验收

- HUMAN：前端“AI 采集”粘贴原文，生成 Draft，在“审核工作箱”修订并发布；
- AGENT：调用 `POST /api/v2/capture/agent`，确认草稿进入 Review Inbox；
- 权限：用 AGENT token 读取 `/api/v2/drafts/{id}` 应返回 403；
- 追溯：发布后应能查询 `exp_draft_publication` 和 `exp_experience_summary`；
- 失败：关闭 LLM 或使用错误地址时，原始 Candidate 仍存在，Draft 显示 `GENERATION_FAILED` 并可重试。

完整接口与设计见 `docs/ai-authoring-v1.2.md`。
